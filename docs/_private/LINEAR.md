# Linear (private reference)

This file is intentionally gitignored via `docs/_private/` in `.gitignore`.
Put any sensitive Linear details here (project URL, IDs, internal notes) instead of `AGENTS.md`.

## Project
- Name:
- Workspace / Team:
- Project URL:
- Project ID (optional):

## Quick search terms
- `ai-automation-consulting-site`
